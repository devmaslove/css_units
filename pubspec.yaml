name: css_units
description: A conversion library that helps implement CSS Viewport Units
version: 0.0.1
repository: https://github.com/dmitrymaslovhome/css_units

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
