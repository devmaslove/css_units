name: css_units
description: A conversion library that helps implement CSS Viewport Units
version: 0.0.4
repository: https://github.com/devmaslove/css_units

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
